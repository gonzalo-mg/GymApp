
import "./index.css";
import {UserCard} from "../UserCard"

export const Header = () => {
//const location = useLocation();
  return (
    <header>
      <h2>Gym App: exercises library</h2>
    </header>
  );
};
